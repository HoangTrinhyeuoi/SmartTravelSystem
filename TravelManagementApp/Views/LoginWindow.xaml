<Window x:Class="TravelManagementApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Smart Travel System - Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Smart Travel System"
                   FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <Label Content="Code:" Width="100"/>
            <TextBox x:Name="txtCode" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <Label Content="Password:" Width="100"/>
            <PasswordBox x:Name="txtPassword" Width="200"/>
        </StackPanel>

        <Label x:Name="lblError" Grid.Row="3" 
               Foreground="Red" 
               HorizontalAlignment="Center"
               Margin="0,10,0,0"/>

        <Button x:Name="btnLogin" Grid.Row="4" 
                Content="Login" 
                Width="150" 
                Height="35"
                FontSize="14"
                Margin="0,20,0,0"
                Click="BtnLogin_Click"/>
    </Grid>
</Window>